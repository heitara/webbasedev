<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap      
    PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"      
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="servicePoint">
	<typeAlias alias="servicePoint" type="com.gameif.portal.entity.ServicePoint" />
	
	<resultMap class="com.gameif.portal.entity.ServicePoint" id="servicePointResult">
		<result property="servicePointNo" column="service_point_no" />
		<result property="memNum" column="mem_num" />
		<result property="giveDate" column="give_date" />
		<result property="pointStartDate" column="point_start_date" />
		<result property="pointEndDate" column="point_end_date" />
		<result property="titleId" column="title_id" />
		<result property="pointAmount" column="point_amount" />
		<result property="createdDate" column="created_date"/>
		<result property="createdUser" column="created_user"/>
		<result property="lastUpdateDate" column="last_update_date"/>
		<result property="lastUpdateUser" column="last_update_user"/>
	</resultMap>
	
	<insert id = "save" parameterClass="servicePoint"> 
        <![CDATA[
			INSERT INTO service_point
				(mem_num,
				give_date,
				point_start_date,
				point_end_date,
				title_id,
				point_amount,
				created_date,
				created_user,
				last_update_date,
				last_update_user)
			VALUES
				(#memNum#,
				#giveDate#,
				#pointStartDate#,
				#pointEndDate#,
				#titleId#,
				#pointAmount#,
				#createdDate#,
				#createdUser#,
				#lastUpdateDate#,
				#lastUpdateUser#)
        ]]>
		<selectKey resultClass="java.lang.Long" keyProperty="servicePointNo">   
        	<![CDATA[
			SELECT last_insert_id() as service_point_no
        	]]>
		</selectKey>
	</insert>

	<select id="selectBalanceByTitleAndMemnum" parameterClass="servicePoint" resultMap="servicePointResult">   
        <![CDATA[
	        SELECT
				service_point_no,
				mem_num,
				give_date,
				point_start_date,
				point_end_date,
				title_id,
				point_amount,
				created_date,
				created_user,
				last_update_date,
				last_update_user
			FROM
				service_point
			WHERE
				mem_num = #memNum#
			AND title_id = #titleId#
      		AND (#giveDate# BETWEEN point_start_date AND point_end_date)
        ]]>
	</select>

	<update id="update" parameterClass="servicePoint">
		<![CDATA[
			UPDATE service_point
			SET	mem_num = #memNum#,
				give_date = #giveDate#,
				point_start_date = #pointStartDate#,
				point_end_date = #pointEndDate#,
				title_id = #titleId#,
				point_amount = #pointAmount#,
				created_date = #createdDate#,
				created_user = #createdUser#,
				last_update_date = #lastUpdateDate#,
				last_update_user = #lastUpdateUser#
			WHERE
				service_point_no = #servicePointNo#
		]]>
	</update>

</sqlMap>
